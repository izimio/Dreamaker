import { FC } from "react";

const InteractionButtons: FC = () => {
    return (
        <div>
            <button>Like</button>
            <button>Comment</button>
            <button>Share</button>
        </div>
    );
};

export default InteractionButtons;
