import { Box } from "@chakra-ui/react";
import { FC } from "react";

const Dreams: FC = () => {
    return (
        <Box>
            <h1>Dreams</h1>
        </Box>
    );
};

export default Dreams;
