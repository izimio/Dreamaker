import { Box } from "@chakra-ui/react";
import { FC } from "react";

const Profile: FC = () => {
    return (
        <Box>
            <h1>Profile</h1>
        </Box>
    );
};

export default Profile;
